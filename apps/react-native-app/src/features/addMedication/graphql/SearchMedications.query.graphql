query SearchMedications($query: String!, $after: String) {
  searchMedications(query: $query, after: $after) {
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
        activeIngredient
        brandName
        strength
      }
    }
  }
}
