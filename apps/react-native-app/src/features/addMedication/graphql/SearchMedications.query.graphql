query SearchMedications($query: String!) {
  searchMedications(query: $query) {
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
        activeIngredient
        brandName
        dosageForms
        strengthsAvailable
      }
    }
  }
}
