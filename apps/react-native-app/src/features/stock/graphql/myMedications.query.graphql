query MyMedications($after: String) {
  myMedications(after: $after) {
    edges {
      node {
        ...MyMedicationBase
        schedule {
          id
          scheduledDays
          timesPerDay
          dosesRemaining
        }
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
